"/* To be implemented. For now, clean pass. \n\n   Input:  the request body — {\"records\":[...]} (or a bare array).\n   Output: [record] — unchanged entries. */\n\nif std.isArray(body) then body else body.records\n"
