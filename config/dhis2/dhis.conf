connection.dialect = org.hibernate.dialect.PostgreSQLDialect
connection.driver_class = org.postgresql.Driver
connection.url = jdbc:postgresql://db:5432/dhis
connection.username = dhis
connection.password = dhis

# Allow the DHIS2 "Route" feature to call the mediator (used later if we
# expose the mediator through DHIS2 instead of calling it directly).
route.remote_servers_allowed = http://mediator:9070
